<% include ./partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">
        New Blog
    </div>
    <form class="ui form" action="/blogs" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" name="blog[title]" value="" placeholder="title" required>
        </div>
        <div class="field">
            <label>Image</label>
            <input type="text" name="blog[image]" value="" placeholder="image" required>
        </div>
        <div class="field">
            <label>Blog Content</label>
            <textarea rows="20" cols="" name="blog[body]"></textarea>
        </div>
        <button type="submit" class="ui purple basic button">Submit</button>
    </form>
</div>

<% include ./partials/footer%>